# 비밀값만 로컬 전용 (.env 는 절대 커밋하지 않음)
.env
# sync_dotenv_asset.ps1 가 복사하는 번들 — API 키 유출 방지
assets/env/dotenv

# --- 커밋하지 않음: 도구·IDE·로컬 테스트·빌드 산출물 ---
# lib/data/DUR1 ~ DUR2 은 CSV 저장용으로 저장소에 포함함 (여기 무시 규칙 적용 안 함).

# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ / Android Studio
*.iml
*.ipr
*.iws
.idea/

# Flutter / Dart / Pub
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
coverage/

# SUBST(한글 경로) 빌드 — tool/run_with_ascii_path.ps1 가 프로젝트 루트에 둠
.link26_gradle_user/
.link26_tmp/

# Symbolication / obfuscation
app.*.symbols
app.*.map.json

# Android build outputs
*.apk
!docs/link26.apk
*.aab
/dist/
app-debug.apk
/android/app/debug
/android/app/profile
/android/app/release

# Kotlin 컴파일러 세션 임시 파일(AGP가 android/.kotlin/sessions 에 생성 — 커밋·스테이징 대상 아님)
android/.kotlin/

# Node (BFF)
bff/node_modules/
