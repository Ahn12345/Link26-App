// 에뮬/기기에 이 저장소 빌드가 올라왔는지 확인할 때 사용합니다.

abstract final class BuildIdentity {
  /// [pubspec.yaml] `version` 과 맞출 것.
  static const marker = 'Link26 · 빌드 1.0.0+3';

  static const openFrom =
      r'Cursor/AS: Desktop\Work\Link26-App → GitHub 동일 폴더';
}
