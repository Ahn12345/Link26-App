import 'package:flutter/material.dart';

class TextSizeSettingTile extends StatelessWidget {
  const TextSizeSettingTile({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('text_size_setting_tile')),
      body: const Center(child: Text('TODO UI')),
    );
  }
}
