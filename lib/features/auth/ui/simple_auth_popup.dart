import 'package:flutter/material.dart';

class SimpleAuthPopup extends StatelessWidget {
  const SimpleAuthPopup({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('simple_auth_popup')),
      body: const Center(child: Text('TODO UI')),
    );
  }
}
